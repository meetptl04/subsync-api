Project name : subsync-api

npx express-generator --no-view --git ./

npm install --save-dev nodemon

npx eslint --init

npm install jsonwebtoken bcryptjs

npm install @upstash/workflow

npm install dayjs

npm install nodemailer

npx @upstash/qstash-cli dev
